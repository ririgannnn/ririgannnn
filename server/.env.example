# PostgreSQL 数据库连接字符串（从 Neon 获取）
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# JWT 密钥（随机字符串，用于登录令牌加密）
JWT_SECRET=change-me-to-a-random-string

# 服务器端口（Railway 会自动提供 PORT，本地开发用 3001）
PORT=3001
